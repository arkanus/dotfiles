[alias]
	br = branch -vv
	co = checkout
	ci = commit
	sh = stash
	lg = log --oneline --decorate
	tree = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)%Creset' --abbrev-commit
	logf = log --stat
	logu = log @{u}..
	st = status -sb
	diffc = diff --cached
	n = number
[user]
	name = Marcos Sanchez
	email = arkanus@gmail.com

	# this is safe to be public https://security.stackexchange.com/questions/153579/is-it-safe-to-share-your-gitconfigs-user-signingkey-value-with-the-world
	signkey = 66DD1A7
[push]
	default = tracking
[color]
	ui = true
[core]
	excludesfile = ~/.gitignore_global
	pager = less -F -X
[github]
	user = arkanus
[grep]
	lineNumber = true
[url "git@github.com:"]
	insteadOf = https://github.com/
[gpg]
	program = gpg2
[commit]
	gpgsign = false
